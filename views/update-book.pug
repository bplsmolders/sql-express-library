extends layout

block content
    h1.block= title
    
    if(errors)
        h2.errors Oops an error occurred!
        ul.errors
            each error in errors
                li=error.message

    form.container(action=`/books/${book.id}`, method="post")
        p
            label(for='title') Title
            input#title(name='title', type='text', value=book.title)
        p
            label(for='author') Author
            input#author(name='author', type='text', value=book.author)
        p
            label(for='genre') Genre
            input#genre(name='genre', type='text', value=book.genre)
        p
            label(for='year') Year
            input#year(name='year', type='text', value=book.year)
        p
            input(type='submit', value='Update Book')
        p
            a.button(href='/') Cancel
        
            a.buttonsearch(href=`/books/${book.id}/delete`) Delete Book

            
